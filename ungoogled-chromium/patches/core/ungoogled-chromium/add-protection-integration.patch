--- a/chrome/browser/browser_process_impl.cc
+++ b/chrome/browser/browser_process_impl.cc
@@ -15,6 +15,7 @@
 #include "components/ungoogled/core_automation/controller_manager.h"
 
 void BrowserProcessImpl::Initialize() {
+  // Initialize protection systems
+  ungoogled::ControllerManager::GetInstance()->InitializeControllers();
   
   // Continue with normal initialization
   StartMetricsService();
@@ -245,6 +246,9 @@ void BrowserProcessImpl::PreCreateThreads() {
   // Initialize thread-specific components
   InitializeThreadSpecificComponents();
+
+  // Configure protection level
+  ungoogled::ControllerManager::GetInstance()->ConfigureProtectionLevel(
+      ungoogled::ProtectionLevel::MAXIMUM);
 }
